<%= form_with(model: office_location, local: true) do |form| %>
  <% if office_location.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(office_location.errors.count, "error") %> prohibited this office_location from being saved:</h2>

      <ul>
        <% office_location.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :street_address %>
    <%= form.text_field :street_address %>
  </div>

  <div class="field">
    <%= form.label :zip %>
    <%= form.text_field :zip %>
  </div>

  <div class="field">
    <%= form.label :phone %>
    <%= form.text_field :phone %>
  </div>

  <div class="field">
    <%= form.label :physician_id %>
    <%= form.text_field :physician_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
